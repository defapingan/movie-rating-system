<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Movie Rating System" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <!-- 加载CSS - 先尝试编译的，再加载CDN作为回退 -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
    
    <!-- Bootstrap CDN作为回退 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">

    <!-- Importmap JS -->
    <%= javascript_importmap_tags %>
    
    <!-- 加载自定义JS -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <style>
/* 最终极的Modal样式覆盖 */
#deleteConfirmationModal .modal-content {
  background: linear-gradient(145deg, #1f2937 0%, #0f172a 100%) !important;
  border: 2px solid #7e22ce !important;
  color: #f8fafc !important;
}

#deleteConfirmationModal .btn-delete {
  background: linear-gradient(45deg, #7e22ce, #1e3a8a) !important;
  border: none !important;
  color: white !important;
}
</style>
  </head>

  <body>
    <%= yield %>
    
    <!-- Bootstrap JS CDN（确保在body末尾加载） -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" 
            crossorigin="anonymous"></script>
    
    <!-- 确保我们的JS能访问bootstrap -->
    <script>
      // 检查Bootstrap是否加载
      document.addEventListener('DOMContentLoaded', function() {
        console.log('Bootstrap loaded:', typeof bootstrap !== 'undefined');
        
        // 如果Bootstrap未加载，显示警告
        if (typeof bootstrap === 'undefined') {
          console.error('Bootstrap not loaded!');
          // 可以在这里添加错误处理
        }
      });
    </script>
  </body>
</html>